import { Typography, Card } from 'antd';

export default function ImportExport() {
  return (
    <div>
      <Typography.Title level={4}>导入导出</Typography.Title>
      <Card><p>请前往「净值管理」页面进行净值数据导入。批量导出功能将在后续版本实现。</p></Card>
    </div>
  );
}
